import pandas as pd
if __name__=="__main__":
    # df1 = pd.DataFrame([['a', 1], ['b', 2]],columns=['letter', 'number'])
    # df2 = pd.DataFrame([['c', 3], ['d', 4]],columns=['lettera', 'number'])
    # ad = pd.concat([df1, df2])
    # print(ad)
    # all = [1,2]
    # ad = pd.DataFrame([[i for i in all], ['d', 4]],columns=['lettera', 'number'])
    x = 10
    if x in range(7, 11):
        print("keren")
    elif x in range(4, 7):
        print("b")
    elif x in range(0,4):
        print("cct")
    